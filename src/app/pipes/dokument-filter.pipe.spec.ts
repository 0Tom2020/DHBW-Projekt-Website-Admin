import { DokumentFilterPipe } from './dokument-filter.pipe';

describe('DokumentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DokumentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
